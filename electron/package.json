{
  "_comment": "Also consider changing the web/package.json",
  "name": "blackbird",
  "version": "0.0.1",
  "description": "TODO",
  "main": "web/dev_entry.js",
  "scripts": {
    "build": "npm run icons && pub run build_runner build --config=release -o build",
    "build_runner_serve": "pub run build_runner serve --hot-reload",
    "dev": "run-p \"build_runner_serve\" \"start\"",
    "icons": "png2icons web/icon.png web/icon -allp -bc -i",
    "start": "electron .",
    "install": "electron-rebuild"
  },
  "dependencies": {
    "electron": "^1.8.8",
    "ngx-ui-switch": "^8.0.0-beta.0",
    "png2icons": "^1.0.1",
    "serialport": "^7.1.1"
  },
  "devDependencies": {
    "less": "^3.9.0",
    "npm-run-all": "^4.1.5",
    "electron-rebuild": "^1.4.0"
  }
}
